import SwiftUI

struct ContentView: View {
    var body: some View {
        TrainCars()
            .frame(width: 350)
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
